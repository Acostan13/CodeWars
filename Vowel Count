function getCount(str) {
  var vowelsCount = 0;
  var vowel_list = 'aeiou';
  
  for(let i = 0; i < str.length ; i++){
    if (vowel_list.indexOf(str[i]) !== -1){
      vowelsCount++;
    }
  }
  return vowelsCount;
}